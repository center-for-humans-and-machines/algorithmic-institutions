# AUTOGENERATED by scripts/rl_two_worlds/make_configs.py -- do not hand-edit.
#
# COST PILOT -- a wall-clock measurement, not a result. 40 update steps, run
# only to price a full 4000-step run before committing three A100-days. Its
# reward mode and the unfixed free-punishment defect do not affect timing, and
# nothing it produces is science. Do not read its policy.
#
# RL manager vs the current corrected clones (rl_new_clones_pilot).
# Every key below except `seed`, `job_id` and `output_dir` is byte-identical
# across the three runs, and the hyperparameters are those of
# configs/training/rl_manager/03_2g8a_sum.yml -- nothing is tuned.

artificial_humans: artifacts/artificial_humans/group_switching_contribution_50ep_vnode_stimulus_skip_herding_copula/model/architecture_node+edge+rnn__dataset_50ep__epochs_575.pt
artificial_humans_valid: artifacts/artificial_humans/raven_script_22/model/rnn_False__dataset_full.pt
switch_model: artifacts/artificial_humans/switch_joint_exodus/model/architecture_mlp+rnn+edge__dataset_50ep_doubled.pt
opponent_manager: artifacts/baselines/punishment_multinomial_ceiling_severity_copula.joblib
artificial_humans_model: "graph"
job_id: rl_new_clones_pilot
seed: 42

manager_args:
  opt_args:
      lr: 2.e-4
  gamma: 0.98
  eps: 0.1
  target_update_freq: 1000
  model_args:
      hidden_size: 100
      add_rnn: true
      add_edge_model: true
      add_global_model: False
      x_encoding:
        - name: contribution
          n_levels: 21
          encoding: numeric
        - name: prev_punishment
          n_levels: 31
          encoding: numeric
        - etype: bool
          name: contribution_valid
        - etype: bool
          name: in_group
      b_encoding:
        - name: round_number
          n_levels: 32
          encoding: onehot

replay_memory_args:
  n_episodes: 100

n_update_steps: 40
eval_period: 10
training_batch_size: 1

env_args:
  n_groups: 2
  n_agents: 8
  agent_groups: [0, 0, 0, 0, 1, 1, 1, 1]
  rl_group_id: 0
  switch_every: 4
  n_contributions: 21
  n_punishments: 31
  n_rounds: 24
  batch_size: 1000
  reward_mode: sum

device: "cuda"
output_dir: "artifacts/manager/rl_new_clones_pilot"
basedir: "."
