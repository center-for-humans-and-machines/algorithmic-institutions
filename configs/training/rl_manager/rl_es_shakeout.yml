# AUTOGENERATED by scripts/rl_es/make_configs.py -- do not hand-edit.
#
# COST PILOT -- a wall-clock and signal-to-noise measurement, not a result.
# Nothing it produces is science; do not read its policy as an outcome.
#
# EVOLUTION STRATEGIES arm of the four-way exploration comparison
# (Salimans, Ho, Chen, Sidor and Sutskever, 2017). One variable changes
# against configs/training/rl_manager/rl_new_clones_s42.yml: the behaviour
# policy. There is NO action noise anywhere in this arm. Every policy that is
# ever run or scored is a fixed deterministic policy evaluated over complete
# episodes; the population members are the behaviour policies and the mean
# parameter vector is the evaluated one.
#
# BUDGET. Equal environment EPISODES, not update steps. One generation scores
# the whole population inside a single env rollout of batch_size episodes, so
# a generation consumes exactly the 1000 behaviour episodes that one DQN
# update step consumes, and `n_generations` equals the reference config's
# `n_update_steps`. Behaviour budget 200000 episodes; evaluation adds
# 10 deterministic rollouts of 1000, at the same intervals as the
# DQN arm.
#
# AXES. The generation index is written into the metrics parquet's
# `update_step` column. It is NOT a gradient step -- this method has none. It
# is an episode counter, and it counts the same episodes the DQN arm's
# `update_step` counts.

artificial_humans: artifacts/artificial_humans/group_switching_contribution_50ep_vnode_stimulus_skip_herding_copula/model/architecture_node+edge+rnn__dataset_50ep__epochs_575.pt
artificial_humans_valid: artifacts/artificial_humans/raven_script_22/model/rnn_False__dataset_full.pt
switch_model: artifacts/artificial_humans/switch_joint_exodus/model/architecture_mlp+rnn+edge__dataset_50ep_doubled.pt
opponent_manager: artifacts/baselines/punishment_multinomial_ceiling_severity_copula.joblib
artificial_humans_model: graph
manager_args:
  opt_args:
    lr: 0.0002
  gamma: 0.98
  eps: 0.1
  target_update_freq: 1000
  model_args:
    hidden_size: 100
    add_rnn: true
    add_edge_model: true
    add_global_model: false
    x_encoding:
    - name: contribution
      n_levels: 21
      encoding: numeric
    - name: prev_punishment
      n_levels: 31
      encoding: numeric
    - etype: bool
      name: contribution_valid
    - etype: bool
      name: in_group
    b_encoding:
    - name: round_number
      n_levels: 32
      encoding: onehot
env_args:
  n_groups: 2
  n_agents: 8
  agent_groups:
  - 0
  - 0
  - 0
  - 0
  - 1
  - 1
  - 1
  - 1
  rl_group_id: 0
  switch_every: 4
  n_contributions: 21
  n_punishments: 31
  n_rounds: 24
  batch_size: 1000
  reward_mode: common_pool
device: cuda
basedir: .
job_id: rl_es_shakeout
seed: 42
n_generations: 200
eval_period: 20
es_args:
  population_size: 40
  sigma: 0.05
  lr: 0.001
  l2_coeff: 0.005
  mirrored: true
  fitness_shaping: centered_rank
output_dir: artifacts/manager/rl_es_shakeout
