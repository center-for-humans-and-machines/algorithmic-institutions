# AUTOGENERATED by scripts/rl_anneal_local/make_configs.py -- do not hand-edit.
#
# GUARD PILOT -- not a result. 300 update steps, run only to show the
# behaviour-versus-evaluated punishment gap at the start and at the end of an
# anneal, and the policy shape the two rollouts produce. Do not read its
# policy as an outcome; 300 steps is 7.5% of a real run.
#
# RL manager vs the current corrected clones (rl_anneal_local_pilot).
# Every key below except `seed`, `job_id` and `output_dir` is byte-identical
# across the five runs.

artificial_humans: artifacts/artificial_humans/group_switching_contribution_50ep_vnode_stimulus_skip_herding_copula/model/architecture_node+edge+rnn__dataset_50ep__epochs_575.pt
artificial_humans_valid: artifacts/artificial_humans/raven_script_22/model/rnn_False__dataset_full.pt
switch_model: artifacts/artificial_humans/switch_joint_exodus/model/architecture_mlp+rnn+edge__dataset_50ep_doubled.pt
opponent_manager: artifacts/baselines/punishment_multinomial_ceiling_severity_copula.joblib
artificial_humans_model: "graph"
job_id: rl_anneal_local_pilot
seed: 42

manager_args:
  opt_args:
      lr: 2.e-4
  gamma: 0.98
  eps: 0.1
  eps_final: 0.01
  eps_anneal_steps: 225
  explore_sigma: 2.0
  target_update_freq: 1000
  model_args:
      hidden_size: 100
      add_rnn: true
      add_edge_model: true
      add_global_model: False
      x_encoding:
        - name: contribution
          n_levels: 21
          encoding: numeric
        - name: prev_punishment
          n_levels: 31
          encoding: numeric
        - etype: bool
          name: contribution_valid
        - etype: bool
          name: in_group
      b_encoding:
        - name: round_number
          n_levels: 32
          encoding: onehot

replay_memory_args:
  n_episodes: 100

n_update_steps: 300
eval_period: 20
training_batch_size: 1

env_args:
  n_groups: 2
  n_agents: 8
  agent_groups: [0, 0, 0, 0, 1, 1, 1, 1]
  rl_group_id: 0
  switch_every: 4
  n_contributions: 21
  n_punishments: 31
  n_rounds: 24
  batch_size: 1000
  reward_mode: common_pool

device: "cuda"
output_dir: "artifacts/manager/rl_anneal_local_pilot"
basedir: "."
